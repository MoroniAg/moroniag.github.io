version: "3.8"
services:
  api:
    build: .
    image: portfolio-api:latest
    env_file: .env
    ports:
      - "${APP_PORT:-8000}:${APP_PORT:-8000}"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
